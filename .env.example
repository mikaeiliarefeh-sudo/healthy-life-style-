# Use mock AI responses (no API calls, no cost) — set to false for real Claude calls
VITE_USE_MOCK=true

# Server-side only (set in your hosting provider's dashboard, e.g. Vercel project settings).
# Used by api/claude.ts — never exposed to the browser.
ANTHROPIC_API_KEY=
